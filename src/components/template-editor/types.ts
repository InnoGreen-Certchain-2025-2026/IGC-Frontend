export type EditorMode = "select" | "text-select" | "draw";

export type FieldType = "text" | "date" | "number" | "image";

export type {
  TemplateField,
  TemplateFieldAlign,
  TemplateFieldRequest,
} from "@/types/template";
